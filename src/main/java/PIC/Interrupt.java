package PIC;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class Interrupt {

    private static final Logger log = LogManager.getLogger(Interrupt.class);

}
